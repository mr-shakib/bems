"use client";

import { SettingsLoading } from "@/components/loading-components";

const SettingsLoadingPage = () => {
  return <SettingsLoading isVisible={true} message="Loading Settings..." />;
};

export default SettingsLoadingPage;